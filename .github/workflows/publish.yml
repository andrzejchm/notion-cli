name: Publish to npm

on:
  push:
    tags:
      - 'v*'

jobs:
  ci:
    name: Typecheck, lint & test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/ci

  publish:
    name: Publish to npm
    needs: ci
    runs-on: ubuntu-latest
    permissions:
      contents: write  # needed to create GitHub releases
      id-token: write  # for npm provenance

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # full history needed for changelog generation

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Publish to npm
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Generate changelog
        id: changelog
        run: |
          CURRENT_TAG="${GITHUB_REF_NAME}"
          PREV_TAG=$(git tag --sort=-version:refname | grep -v "^${CURRENT_TAG}$" | head -1)

          if [ -z "$PREV_TAG" ]; then
            COMMITS=$(git log --pretty=format:"%s" HEAD)
          else
            COMMITS=$(git log --pretty=format:"%s" "${PREV_TAG}..HEAD")
          fi

          OUT=/tmp/release-notes.md
          > "$OUT"

          write_section() {
            local header="$1" type="$2"
            local lines
            lines=$(echo "$COMMITS" | grep -E "^${type}(\(.+\))?(!)?:" | sed "s/^${type}[^:]*: /- /" || true)
            if [ -n "$lines" ]; then
              echo "$header" >> "$OUT"
              echo "$lines" >> "$OUT"
              echo "" >> "$OUT"
            fi
          }

          write_section "### Features"      feat
          write_section "### Bug Fixes"     fix
          write_section "### Performance"   perf
          write_section "### Refactoring"   refactor
          write_section "### Documentation" docs
          write_section "### Chores"        chore
          write_section "### CI"            ci
          write_section "### Tests"         test

          # Uncategorized commits
          UNCATEGORIZED=$(echo "$COMMITS" | grep -vE "^(feat|fix|perf|refactor|docs|chore|ci|test)(\(.+\))?(!)?:" || true)
          if [ -n "$UNCATEGORIZED" ]; then
            echo "### Other" >> "$OUT"
            echo "$UNCATEGORIZED" | sed 's/^/- /' >> "$OUT"
            echo "" >> "$OUT"
          fi

          if [ ! -s "$OUT" ]; then
            echo "No notable changes." >> "$OUT"
          fi

          # Contributors (exclude repo owner and bots)
          if [ -z "$PREV_TAG" ]; then
            AUTHORS=$(git log --pretty=format:"%an|%ae" HEAD)
          else
            AUTHORS=$(git log --pretty=format:"%an|%ae" "${PREV_TAG}..HEAD")
          fi

          CONTRIBUTORS=$(echo "$AUTHORS" \
            | sort -u \
            | grep -v '\[bot\]' \
            | grep -v 'dependabot' \
            | grep -vi 'github-actions' \
            | while IFS='|' read -r name email; do
                # look up GitHub username from email
                login=$(curl -sf -H "Authorization: token ${{ github.token }}" \
                  "https://api.github.com/search/users?q=${email}+in:email" \
                  | jq -r '.items[0].login // empty' 2>/dev/null || true)
                if [ -n "$login" ] && [ "$login" != "${{ github.repository_owner }}" ]; then
                  echo "@${login}"
                fi
              done \
            | sort -u)

          if [ -n "$CONTRIBUTORS" ]; then
            echo "### Contributors" >> "$OUT"
            echo "Thanks to $(echo "$CONTRIBUTORS" | tr '\n' ' ' | sed 's/ *$//; s/  */ /g; s/ \(@[^ ]*\)$/ and \1/; s/ /, /g; s/, and / and /') for contributing to this release! :tada:" >> "$OUT"
            echo "" >> "$OUT"
          fi

          if [ -n "$PREV_TAG" ]; then
            COMPARE_URL="https://github.com/${{ github.repository }}/compare/${PREV_TAG}...${CURRENT_TAG}"
            echo "**Full diff:** ${COMPARE_URL}" >> "$OUT"
          fi

          echo "prev_tag=$PREV_TAG" >> "$GITHUB_OUTPUT"

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            --title "$GITHUB_REF_NAME" \
            --notes-file /tmp/release-notes.md
